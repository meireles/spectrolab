% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/descrip_summary_overloads.R
\name{is.na.spectra}
\alias{is.na.spectra}
\title{Find missing values in a spectra object}
\usage{
\method{is.na}{spectra}(x)
}
\arguments{
\item{x}{spectra object}
}
\value{
logical matrix, same shape as \code{value(x)}
}
\description{
\code{is.na} checks the value matrix for \code{NA}s, element by element.
Without this method, \code{is.na()} would check spectra's four internal
slots instead (always returning 4 \code{FALSE}s), which looks like "no
missing data" regardless of what the value matrix actually contains.
}
\examples{
library(spectrolab)
spec = as_spectra(spec_matrix_example, name_idx = 1)
any(is.na(spec))
}
\author{
Jose Eduardo Meireles
}
