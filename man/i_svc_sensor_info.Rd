% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensor_info.R
\name{i_svc_sensor_info}
\alias{i_svc_sensor_info}
\title{Read SVC overlap/matching provenance for a set of `.sig` files}
\usage{
i_svc_sensor_info(file_paths, max_header_lines = 40)
}
\arguments{
\item{file_paths}{character vector of `.sig` paths}

\item{max_header_lines}{how many header lines to scan (defaults to 40)}
}
\value{
canonical sensor_info data.frame, \code{length(file_paths)} rows
}
\description{
\code{i_svc_sensor_info} scans the header of each file for the \code{factors=}
line and returns a canonical \code{sensor_info} data.frame (one row per file,
in the order given). This is cheap (a handful of header lines per file) and is
run unconditionally for SVC reads, independently of \code{extract_metadata}.
}
\author{
Jose Eduardo Meireles
}
\keyword{internal}
