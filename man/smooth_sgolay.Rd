% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/savitzky_golay.R
\name{smooth_sgolay}
\alias{smooth_sgolay}
\title{Smooth spectra with a Savitzky-Golay filter}
\usage{
smooth_sgolay(x, p = 3, n = p + 3 - p\%\%2)
}
\arguments{
\item{x}{spectra object. bands must be strictly increasing}

\item{p}{polynomial order. Defaults to 3}

\item{n}{filter length (number of bands in the moving window). Must be odd
and greater than p. Defaults to \code{p + 3 - p \%\% 2}}
}
\value{
spectra object with Savitzky-Golay smoothed spectra
}
\description{
\code{smooth_sgolay} fits a local polynomial of order \code{p} over a moving
window of \code{n} bands and replaces each band value with the fitted value.
Requires the \code{signal} package.
}
\examples{
library(spectrolab)
spec = as_spectra(spec_matrix_example, name_idx = 1)
if(requireNamespace("signal", quietly = TRUE)){
    spec_sg = smooth_sgolay(spec)
}
}
\author{
Jose Eduardo Meireles
}
