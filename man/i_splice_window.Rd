% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/splice.R
\name{i_splice_window}
\alias{i_splice_window}
\title{Crossover window for one junction}
\usage{
i_splice_window(
  w,
  left,
  right,
  splice_at,
  overlap,
  half_width,
  inset,
  window = NULL
)
}
\arguments{
\item{w}{band vector}

\item{left, right}{column indices of the two segments}

\item{splice_at}{this junction's wavelength}

\item{overlap}{logical, is there a physical overlap?}

\item{half_width}{half width used when there is no overlap}

\item{inset}{fraction of the overlap dropped from each end}

\item{window}{explicit window, or NULL}
}
\value{
numeric c(lo, hi)
}
\description{
With a physical overlap the window is the overlap itself, inset off both ends.
Without one there is nothing to measure, so the window is a small interval
around the splice point (\code{half_width}, i.e. \code{interpolate_wvl}), or an
explicit window recorded by the instrument.
}
\keyword{internal}
